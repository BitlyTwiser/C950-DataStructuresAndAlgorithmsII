# Read distance table making a mutli-dimenstional array using the header data from the csv for one array and using the distance values fro mthe other
# Then index into one aray with the other values to find distance
class AddressDistances:
    def __init__(self) -> None:
        self.address
        self.distance_from_hub
